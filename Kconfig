# Copyright (c) 2025 Kaz Walker, Thermoquad
# SPDX-License-Identifier: Apache-2.0

menuconfig FUSAIN
	bool "Fusain Serial Protocol Library"
	help
	  Enable the Fusain serial protocol encoder/decoder library.
	  This library provides CRC-16-CCITT calculation, packet encoding
	  with byte stuffing, and stateful packet decoding for the Fusain
	  serial communication protocol.

	  Note: This is a pure C library with no Zephyr dependencies.
	  It does NOT require CONFIG_CRC (implements own CRC-16-CCITT).
	  It does NOT require CONFIG_SERIAL (protocol layer only, no UART).

if FUSAIN

config FUSAIN_LOG_LEVEL
	int "Fusain Serial Protocol Log Level"
	default 3
	range 0 4
	help
	  Set the log level for the Fusain serial protocol library.
	  0 = OFF, 1 = ERROR, 2 = WARNING, 3 = INFO, 4 = DEBUG

endif # FUSAIN
